#include <iostream>

int main() {
    int x;
    int y = x * 2;  // Using uninitialized variable x

    // Print the value of y
    std::cout << "Value of y: " << y << std::endl;

    // Indicate successful execution and return 0
    return 0;
}#include <iostream>

int main() {
    // Print the message "Hello, C++!" to the console
    std::cout << "Hello, C++!" << std::endl;

    // Indicate successful execution and return 0
    return 0;
}
